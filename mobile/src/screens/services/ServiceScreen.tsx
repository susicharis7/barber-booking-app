import { View, Text } from 'react-native';

export default function ServiceScreen() {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: '#ffffff',
      }}
    >
      <Text style={{ fontSize: 22, color: '#0f172a' }}>
        Services Screen
      </Text>
    </View>
  );
}
